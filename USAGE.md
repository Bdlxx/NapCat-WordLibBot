# 📖 使用教程 (Usage)

本文档是 [NapCat-WordLibBot](https://github.com/Bdlxx/NapCat-WordLibBot) 安装脚本的完整使用教程。
从零开始，一步步教你部署一个机器人实例。

> 在线查看：[GitHub](https://github.com/Bdlxx/NapCat-WordLibBot/blob/master/USAGE.md)

---

## 📋 目录

- [一、环境准备](#一环境准备)
- [二、安装脚本](#二安装脚本)
- [三、部署新实例](#三部署新实例)
- [四、扫码登录 QQ](#四扫码登录-qq)
- [五、机器人配置 config.json](#五机器人配置-configjson)
- [六、启动 Bot](#六启动-bot)
- [七、把机器人拉进群并开启插件](#七把机器人拉进群并开启插件)
- [八、Web 管理面板](#八web-管理面板)
- [九、日常维护](#九日常维护)
- [十、脚本菜单速览](#十脚本菜单速览)
- [常见问题 FAQ](#常见问题-faq)

---

## 一、环境准备

在一台 Linux 服务器（Ubuntu/Debian/CentOS 均可）上安装以下依赖：

| 依赖 | 用途 | 检查命令 |
|---|---|---|
| **Docker** | 运行 NapCat（QQ 客户端）容器 | `docker --version` |
| **Python 3.10+** | 运行机器人主程序 | `python3 --version` |
| **Git** | 拉取项目与插件 | `git --version` |

> 首次运行安装脚本时会自动检查这些依赖。

---

## 二、安装脚本

### 2.1 一键运行

```bash
bash <(curl -s https://raw.githubusercontent.com/Bdlxx/NapCat-WordLibBot/master/install.sh)
```

### 2.2 脚本保存到用户目录

脚本运行时会**自动把自身保存到 `~/napbot/install.sh`**，之后在任意目录执行 `napbot` 即可进入同样的管理菜单（无需再下载）。

> 通过 `curl | bash` 管道运行时无法自动保存，可在安装脚本的「后续设置」中按提示手动保存。

---

## 三、部署新实例

主菜单选 **「1. 📦 部署新实例」**，然后：

### 第 1 步：输入机器人 QQ 号

输入机器人的 QQ 号（如 `123456789`）作为实例标识。每个 QQ 号对应一个完全独立的实例。

### 第 2 步：脚本自动完成 4 件事

1. **克隆项目** → 创建实例目录 `<脚本目录>/instances/<QQ>/`（以 QQ 号命名，多实例互不干扰）
2. **拉取插件（可选）** → 询问"是否从插件仓库拉取插件"，选"是"后弹出**插件勾选列表**（空格选中），
   可自由选择要安装的插件（选中 JM下载 会自动附带其工作进程 jm_worker.py）；选"否"则跳过，
   之后可在「实例管理 → 更新插件」中补装
3. **配置向导** → 询问以下信息并自动写入 `config.json`：
   - **机器人昵称**（如：小助手）
   - **主人 QQ**（必填，拥有全部管理权限，多个用逗号分隔：`10001,10002`）
4. **创建容器** → 生成 NapCat 配置（HTTP/WS 端口与安全 Token 自动分配）并启动 `napcat_<QQ>` 容器（网络模式可选：Host 直通 / Bridge 端口映射）

部署完成后实例即注册成功，可在「实例管理」中看到。

---

## 四、扫码登录 QQ

1. 主菜单选 **「2. 🗂 实例管理」** → 选择你的实例
2. 选 **「📱 查看二维码」** → 用**手机 QQ** 扫码
3. 等待容器日志出现登录成功提示

> 二维码约 2-3 分钟有效，过期后重新获取即可。
> 这一步让 NapCat 容器以你的机器人 QQ 登录，之后才能收发消息。

---

## 五、机器人配置 config.json

实例目录下的 `instances/<QQ>/config.json` 是机器人主配置（**部署时已自动生成，一般无需修改**）：

```json
{
  "BOT_NAME": "我的机器人",          // 机器人昵称（部署时已填）
  "BOT_QQ": 123456789,               // 机器人 QQ（部署时已填）
  "MASTER_QQ": [10001],              // 主人 QQ，可多个，拥有全部管理权限（部署时已填）
  "NAPCAT_HTTP": "http://127.0.0.1:3000",  // NapCat HTTP 地址（脚本已自动写入）
  "WS_URL": "ws://127.0.0.1:3001/?access_token=xxx",  // WebSocket 地址（脚本已自动写入）
  "ACCESS_TOKEN": "xxx"              // NapCat 访问令牌（脚本已自动写入）
}
```

如需**增删主人 QQ**，直接编辑 `MASTER_QQ` 后重启 Bot 即可。

---

## 六、启动 Bot

回到实例管理菜单 → 「🤖 Bot 管理」→ 启动。观察日志出现以下内容即表示正常运行：

```
WebSocket 连接成功，等待事件...
✓ 词库插件 v1.0.0 — ...
```

断线后会自动重连（5 秒间隔）。

---

## 七、把机器人拉进群并开启插件

1. 用 QQ 把机器人拉进你的群
2. 在群里发送**开启命令**启用插件（仅主人 QQ 有效）

| 插件 | 开启命令 | 关闭命令 | 说明 |
|---|---|---|---|
| 词库 | `开启词库` | `关闭词库` | 关键词回复、签到、自定义昵称、排行榜 |
| 结婚 | `开启结婚` | `关闭结婚` | 群内结婚/离婚系统 |
| 视频解析 | `开启视频解析` | `关闭视频解析` | 视频链接去水印（抖音/B站/快手/小红书/TikTok） |
| JM下载 | `开启jm下载` | `关闭jm下载` | 禁漫本子下载转 PDF 分享 |

> 所有插件在每个群默认关闭，需主人发送对应命令开启（**私聊发送 = 全局开关**）。

### 词库插件常用命令

| 命令 | 说明 |
|---|---|
| `{bot}跟我学` | 添加词条 |
| `{bot}忘掉` | 删除词条 |
| `{bot}回忆一下` | 查询词条 |
| `签到` | 每日签到 |
| `签到排行` | 排行榜 |
| `{bot}赞我` | 让机器人赞你 |

> 命令和回复模板可在 `data/wordlib_config.json` 自定义，变量参考见 [VARIABLES.md](VARIABLES.md)。

---

## 八、Web 管理面板

### 启动面板

```bash
# 在项目根目录（install.sh 所在目录）
python web/api.py
```

访问 `http://<服务器IP>:8080`。

### 设置密码

```bash
bash set_password.sh 面板名 密码
```

（或运行 install.sh → 后续设置 → 按提示配置 systemd 服务 + 密码）

### 面板功能

| 页面 | 功能 |
|---|---|
| 📊 仪表盘 | Bot / NapCat 状态、快捷操作 |
| 🛠️ 运行管理 | Bot 启停、NapCat 二维码查看 |
| ⚙️ 插件配置 | 已安装插件列表与表单配置 |
| 👥 群组开关 | 群×插件开关矩阵，批量操作 |
| 📋 运行日志 | 彩色日志查看器 |

---

## 九、日常维护

| 操作 | 方法 |
|---|---|
| **更新插件** | 实例管理 → 「🔌 更新插件」（从插件仓库拉取最新） |
| **更新项目** | 实例管理 → 重新部署（自动 `git pull`） |
| **重启 Bot** | 群内发「重启」（主人），或实例管理 → Bot 管理 |
| **新增实例** | 重复「三、部署新实例」步骤，每个 QQ 号一个独立实例 |
| **卸载实例** | 实例管理 → 卸载（删除容器，保留项目文件） |

---

## 十、脚本菜单速览

### 主菜单

```
1. 📦 部署新实例      # 新建一个机器人实例
2. 🗂  实例管理        # 管理已部署的实例（容器/Bot/二维码/插件/日志/卸载）
3. 🔗 后续设置        # napbot 命令、Web 面板 systemd 服务
Q. 🚪 退出
```

### 实例管理菜单

```
1. 📊 完整状态        6. 🤖 Bot 管理
2. ▶ 启动 NapCat 容器  7. 📋 查看日志
3. ⏹ 停止 NapCat 容器  8. 🔌 更新插件（从插件仓库拉取）
4. 🔄 重启 NapCat 容器  9. ❌ 卸载实例
5. 📱 查看二维码（扫码登录）
```

### 目录结构

```
<脚本目录>/
├── install.sh              # 安装管理脚本
├── instances/              # 所有实例（以 QQ 号命名）
│   ├── 123456789/          # 实例一：完整项目（main.py、plugins/、data/、config.json）
│   └── registry/           # 实例注册文件 <QQ>.sh
├── web/                    # Web 管理面板
└── templates/              # NapCat 配置模板
```

---

## 常见问题 FAQ

### Q1：容器启动后收不到消息？

- 确认已扫码登录（实例管理 → 查看二维码）
- 确认 Bot 已启动且日志显示 `WebSocket 连接成功`
- 确认机器人已拉进目标群

### Q2：端口冲突？

部署时选择 Bridge 模式可自定义 HTTP/WS/WebUI 三个端口；多个实例请使用不同端口。

### Q3：如何添加第二个主人？

编辑 `instances/<QQ>/config.json` 的 `MASTER_QQ`，多个用逗号分隔，重启 Bot 生效。

### Q4：插件不生效？

所有插件在每个群默认关闭，需主人在群内发送开启命令（如 `开启词库`）。

### Q5：napbot 命令失效？

脚本保存位置在 `~/napbot/install.sh`，确认文件存在且 `/usr/local/bin/napbot` 软链接有效：
```bash
ls -la /usr/local/bin/napbot
```

---

*遇到其他问题可到 [GitHub Issues](https://github.com/Bdlxx/NapCat-WordLibBot/issues) 反馈。*
